langgraph>=0.2.0            # multi-agent orchestration - the StateGraph in backend/query/graph/build.py
langchain-core>=0.3.0       # transitive dep of langgraph - not imported directly anywhere in our code
pydantic>=2.7                # schemas/validation - graph/schemas.py, api/schemas.py
pydantic-settings>=2.3       # loads .env into Settings (backend/shared/config/settings.py)
fastapi>=0.111                # web framework - backend/api/main.py + routes
uvicorn[standard]>=0.30      # ASGI server that runs the FastAPI app
python-multipart>=0.0.9       # required by FastAPI for multipart/form file uploads (PDF upload endpoint)
groq>=0.9.0                   # Groq API client - default LLM provider (llm/groq_provider.py)
openai>=1.40.0                # OpenAI-compatible client - used by the aiaas provider (internal UBS gateway)
httpx>=0.27.0                  # HTTP client passed into the OpenAI client for the aiaas provider (verify=False)
aiaas-auth                     # internal UBS package - Azure AD broker auth for the aiaas provider (see README: Nexus index setup)
sentence-transformers>=3.0.0 # MiniLM embedding model + cross-encoder reranker
rank-bm25>=0.2.2              # BM25Okapi keyword search - retrieval/bm25_index.py
sqlite-vec>=0.1.3            # SQLite extension for vector search (vec0) - backend/shared/storage/vector_store.py
numpy>=1.26                   # embedding math - normalize.py, similarity.py
markdown-it-py>=3.0.0        # Markdown parsing (headings, wikilinks) - ingestion/parsers/markdown_parser.py
pymupdf>=1.24.0               # PDF text extraction (fitz) - ingestion/parsers/pdf_parser.py
pyyaml>=6.0                   # loads llm_config.yaml, retrieval_config.yaml, crag_config.yaml
python-dotenv>=1.0            # used internally by pydantic-settings to read .env
pytest>=8.0                   # test runner for backend/tests/
